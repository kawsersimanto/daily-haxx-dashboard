import { createSlice } from "@reduxjs/toolkit";

export const articleCategorySlice = createSlice({
  name: "article-category",
  initialState: {},
  reducers: {},
});

export const {} = articleCategorySlice.actions;
export const articleCategoryReducer = articleCategorySlice.reducer;
