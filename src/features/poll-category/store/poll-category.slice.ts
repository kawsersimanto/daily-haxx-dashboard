import { createSlice } from "@reduxjs/toolkit";

export const pollCategorySlice = createSlice({
  name: "poll-category",
  initialState: {},
  reducers: {},
});

export const {} = pollCategorySlice.actions;
export const pollCategoryReducer = pollCategorySlice.reducer;
